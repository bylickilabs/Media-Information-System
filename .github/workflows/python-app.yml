name: MIS CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read
  security-events: write   # ✅ notwendig für CodeQL
  actions: read

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: 🛒 Repository auschecken
      uses: actions/checkout@v4

    - name: 🐍 Python einrichten
      uses: actions/setup-python@v5
      with:
        python-version: "3.12"

    - name: 📦 Abhängigkeiten installieren
      run: |
        pip install -r requirements.txt || echo "Keine requirements.txt vorhanden"
        pip install customtkinter requests

    - name: 🔍 Anwendung validieren
      run: |
        python -m py_compile app.py

  codeql-analysis:
    needs: build
    runs-on: windows-latest
    permissions:
      contents: read
      security-events: write   # ✅ erforderlich für Upload
      actions: read

    steps:
    - name: 🛒 Repository auschecken
      uses: actio
